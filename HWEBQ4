#include<iostream>
using namespace std;
int main()
{
    int a;
    int b;
    cout << "enter two numbers:" << endl;
    cin >> a >> b;
    cout << "C=" << (5 * (float)a + 3) / (6 * (float)b + 2);
    return  0;
}
